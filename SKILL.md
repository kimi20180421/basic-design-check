---
name: basic-design-check
description: >
  Upload a UX design work and review it on typography, color, and design
  standards. Annotate the original image to highlight issues and strengths,
  then provide actionable improvement suggestions. Built for UX beginners
  after practice exercises.
---

# Basic Design Check

对用户上传的 UX 设计作品进行批阅。
不是泛泛地说"还不错"，而是逐个问题指出来、逐条告诉你怎么改。

## Workflow

1. 收到用户上传的设计稿图片。
2. 先整体观察：这是什么类型的设计（页面、组件、海报、表单等），整体完成度如何。
3. 按以下维度逐项检查：
   - **排版**：对齐、间距、字号层级、行高、留白、网格感
   - **配色**：色彩对比度、色彩和谐性、可读性、情绪是否匹配场景
   - **规范性**：组件一致性、圆角统一、边距规律、设计系统遵循度
4. 判断每个发现是"问题"还是"亮点"。
5. 在原图上叠加标注层，生成批阅图。
6. 图片生成后，再补充文字版的改进建议。

## Annotation Rules

### 标注手段（按优先级混合使用，不要只用方框）

1. **引导线 + 短句**（主力手段，占 60%）：从目标区域引出一条弧线，末端写一句点评。
2. **尺寸标注线**（用于间距/边距/字号问题）：两条短竖线 + 横线 + 数值，像建筑图纸。
3. **圆角方框**（只用于最关键的问题，每张图最多 2 个）：圈出一个完整区域。
4. **下划线 / 侧边线**（用于文字行或边距问题）：轻量标记，不遮挡内容。
5. **对比色块**（用于配色问题）：在旁边放推荐色卡做对比。
6. **小图标**：✓ 亮点、✗ 问题、💡建议，快速传达判断。

### 颜色体系

| 颜色 | 色值 | 含义 |
|---|---|---|
| 珊瑚红 | #FF6B6B | 需要修改的问题 |
| 琥珀黄 | #FFD43B | 可以更好的建议 |
| 薄荷绿 | #51CF66 | 做得好的亮点 |
| 浅灰 | #ADB5BD | 中性信息（尺寸、辅助线） |

当标注色与背景色接近时，加白色描边确保可读。

### 密度控制

- 每张图标注总数控制在 5-8 条
- 圆角方框最多 2 个，不要满屏都是框
- 不同标注手段混合使用，避免视觉单调
- 标注不能遮挡设计稿的核心内容区域

## Scene Adaptation

根据设计稿类型调整检查重点和标注密度：

| 设计稿类型 | 检查重点 | 标注密度 |
|---|---|---|
| 排版练习 | 对齐、间距、字号层级、行高、留白 | 高（6-10 条） |
| 配色练习 | 对比度、和谐性、可读性、情绪匹配 | 中（4-7 条） |
| 规范化练习 | 组件一致性、圆角、边距、系统遵循 | 中（4-7 条） |
| 综合页面 | 以上全部 + 视觉动线 + 信息层级 | 高（8-12 条） |
| 单个组件 | 内部一致性、状态完整性、可用性 | 低（3-5 条） |

## Hard Rules

- 用户上传图片后，直接生成批阅图，不要先提问、不要先输出纯文字分析。
- 必须基于原图编辑，只叠加标注层，不能重绘、不能改变原图的任何内容。
- 保持原图的分辨率、比例、背景色、所有设计元素的位置不变。
- 每条标注必须指向具体位置，不能写"整体排版有问题"这种笼统评价。
- 问题标注必须附带怎么改，不能只说"这里不对"而不给方向。
- 亮点标注必须说清楚好在哪里，不能只写"不错"或"很好"。
- 不要用专业术语轰炸新人。如果用了术语，紧跟一句大白话解释。
- 多张图片逐张独立批阅，不合并。
- 如果宿主工具只能文生图、不能编辑输入图，先说明限制，不要把重绘的图当成原图批阅。

## Output Format

### 第一部分：批阅图
在原图上叠加标注层，红色标记问题、绿色标记亮点。

### 第二部分：文字版改进建议

```
📋 整体评价
{一句话概括这份作品的完成度和主要方向}

🔴 需要改进
1. {具体问题} → {怎么改}
2. {具体问题} → {怎么改}
3. ...

🟢 做得好的地方
1. {具体亮点} → {为什么好}
2. ...

💡 下一步建议
{1-2 句话，告诉新人接下来可以重点练什么}
```

## Triggering

以下方式均可触发本技能：
1. 技能名调用：`basic-design-check`
2. 自然语言："帮我看看这个设计稿" / "检查一下我的作业" / "review my design"

## Post-generation

批阅完成后，提供 3 个迭代方向供用户选择，例如：
- "只看排版问题"
- "只看配色问题"
- "更严格一点，按高级设计师标准批阅"
